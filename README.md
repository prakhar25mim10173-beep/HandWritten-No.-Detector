# HandWritten-No.-Detector
A machine learning project that recognizes handwritten digits using a CNN trained on MNIST. It preprocesses images, predicts single and multiple digits, displays confidence, and provides a Streamlit interface for easy interaction.
